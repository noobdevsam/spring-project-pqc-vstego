package com.example.stego.gatewayservice.controller;

public class JwkSetController {

    // This controller will expose a GET endpoint (e.g., /.well-known/jwks.json)
    // that returns the public key part of the key pair used to sign the JWTs.
    // Resource servers will fetch this key to validate token signatures.

}
